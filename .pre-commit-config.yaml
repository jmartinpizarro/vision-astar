repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.8
    hooks:
      - id: ruff
        args: ["."] # all files of project
      - id: ruff-format # format
        args: ["."]
